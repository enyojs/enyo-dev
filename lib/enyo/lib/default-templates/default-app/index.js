// This is the default "main" file, specified from the root package.json file
// The ready function is excuted when the DOM and Enyo are ready for usage.

var ready = require('enyo/ready');

ready(function() {
	console.log('Hello World!');
});
